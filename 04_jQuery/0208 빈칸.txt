
서술형

1. UML이 뭐냐?
모델링언어
의사소통 용이
다이어그램으로서 표현
>> 복잡한 프로그램을 도식화해서 표현 가능
- 소프트웨어 개념을 다이어그램으로 그리기 위해 사용하는 표기법


2. UML 다이어그램의 종류?
>> 시퀀스다이어그램(동적)
유스케이스다이어그램(동적)
활동다이어그램(동적)

클래스다이어그램
>> 패키지 다이어그램


3. 유스케이스 다이어그램의 구성요소
액터
유스케이스


4. 유스케이스 관계
일반관계 실선
포함관계 INCLUDE
확장관계 EXTEND
일반화관계? 그룹핑


5. 유스케이스 다이어그램을 보고 설명


**
6. DB 모델링의 설계 3가지 방법
개
논
물

**
7. DB용어 - 모델링 용어
테이블 엔티티
컬럼 속성
행 인스턴스
기본키 주식별자



8. 속성, 인스턴스
>> 속성 : 엔티티에서 관리해야할 최소단위 정보항목 -> EMP_ID,EMP_NAME
인스턴스 :  엔티티의 속성으로 실제로 구현된 값 => 선동일, 201, J5

9. PK(주식별자), FK(외래식별자
PK : 테이블에서 인스턴스 값을 유니크하게 구분할 수 있는 키 >> 구별하는 기준
FK : 외부 테이블과의 관계가 있는 키를 FK >> 연결고리

10. 엔티티(테이블)
관계있는 정보

>> 업무의 관심 대상이 되는 사물이여야 함 .. -=> 직원 급여 상품
두개 이상의 인스턴스를 소유해야 한다 ==> 선동일 노홍철 하이유
마땅한 속성을 소유해야 한다 => 컬럼이 있어야 한다

11. 속성(컬럼)의 명명규칙
의미없는거 X
명확하게 EMP_NAME
EN 이런식으로 ㄴㄴ

>> 의미가 분명해야 한다. => EMP_NAME, A xxx
해당 업무에서 사용하는 이름을 부여한다
서술식이나 약어를 사용하지 않는다. EN(X) -> EMP_NAME
엔티티(테이블)에서 유일하게 식별 가능하도록 지정한다 => 컬럼중복 X


12. ERD
>>일대 다 관계
하나의 게시글에 여러 댓글을 달 수 있다


13. 식별관계 VS 비식별관게
비식별 : 외래키가 포함되고 그냥 별개의 컬럼으로 존재
식별 : 외래키가 포함되고 별개의 칼럼이 아닌 일부가 됌

